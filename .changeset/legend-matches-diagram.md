---
"surrounded-by-slop": patch
---

The diagram legend now lists only the edge styles the diagram actually
contains. A codebase with no import cycles and no type-only imports no longer
advertises those relationships, and the "thicker = used more often" scale
explainer appears only once two edges genuinely differ in weight.
